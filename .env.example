# Required for login and sessions
APP_PASSWORD=replace-with-a-strong-password
FLASK_SECRET_KEY=replace-with-a-long-random-value

# Model providers
OPENROUTER_API_KEY=replace-with-your-openrouter-key
ANTHROPIC_API_KEY=replace-with-your-anthropic-key
# Optional direct-provider and custom OpenAI-compatible routes
# DEEPSEEK_API_KEY=replace-with-your-deepseek-key
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# CUSTOM_OPENAI_API_KEY=replace-with-your-provider-key
# CUSTOM_OPENAI_BASE_URL=https://provider.example/v1
# CUSTOM_OPENAI_ALLOW_NO_KEY=false
# Optional custom-provider cost estimates, USD per 1M tokens
# CUSTOM_OPENAI_INPUT_PRICE_PER_MILLION=0
# CUSTOM_OPENAI_OUTPUT_PRICE_PER_MILLION=0
# CUSTOM_OPENAI_CACHE_PRICE_PER_MILLION=0

# Character provider/model overrides. Saved frontend choices take precedence.
# Providers: openrouter, anthropic, deepseek, custom_openai
# PROVIDER_CHAR1=openrouter
# PROVIDER_CHAR2=openrouter
# PROVIDER_CHAR3=openrouter
# PROVIDER_CHAR4=openrouter
# PROVIDER_CHAR5=anthropic
# PROVIDER_CHAR6=openrouter
MODEL_CHAR1=google/gemini-3-flash-preview
MODEL_CHAR2=openai/gpt-4o-mini
MODEL_CHAR3=google/gemini-3-flash-preview
MODEL_CHAR4=x-ai/grok-4.3
MODEL_CHAR5=claude-sonnet-4-6
MODEL_CHAR6=anthropic/claude-fable-5
SUMMARY_PROVIDER=openrouter
SUMMARY_MODEL=google/gemini-2.5-flash-lite

# Storage
# These defaults are for local Python runs. compose.yaml safely overrides them
# with /data paths inside Docker so the named volume remains persistent.
DB_PATH=becoming.db
# UPLOAD_ROOT=/absolute/path/to/chat_uploads
# BECOMING_MEMORY_DIR=/absolute/path/to/becoming_memory
# MUSIC_LIBRARY_DIR=/absolute/path/to/music_library

# Long-term memory: embedded (default), disabled, or module.path:factory.
# Custom backends only need recall(owner_id) and save(content, owner_id, **metadata).
MEMORY_BACKEND=embedded

# Scheduling
SCHEDULER_TIMEZONE=UTC
# Defaults to SCHEDULER_TIMEZONE; use a separate IANA zone only when needed.
# SLEEP_TIMEZONE=Asia/Shanghai
SCHEDULER_ENABLED=true
DESIRE_DRIVEN=true
DESIRE_TICK_MINUTES=10
GROUP_SUMMARY_THRESHOLD=40

# Optional built-in together-listening library. NETEASE_MUSIC_U is the MUSIC_U
# cookie value from the deployer's own NetEase Cloud Music account. Treat it as
# a server secret; never expose it to frontend code or commit it.
# NETEASE_MUSIC_U=replace-with-your-own-music-u-cookie
# NETEASE_BITRATE=320000

# Optional comma-separated origins allowed to call the password-protected
# /api/sleep/nudge bridge from an external page. Both settings are required.
# SLEEP_NUDGE_ENABLED=false
# CORS_ALLOW_ORIGINS=https://your-static-page.example

# Optional Android/iOS companion push bridge. Disabled by default. Music sync,
# voice, and read-only phone search use an operator-controlled companion or
# custom MCP connection; see docs/MOBILE_EXTENSIONS.md.
# MOBILE_PUSH_ENABLED=false
# MOBILE_PUSH_WEBHOOK_URL=https://companion.example.com/becoming/events
# MOBILE_PUSH_WEBHOOK_SECRET=replace-with-at-least-16-random-characters
# MOBILE_PUSH_TIMEOUT=5

# Optional memory metadata analysis. OPENROUTER_API_KEY is reused when these
# direct-provider keys are empty.
# OMBRE_API_KEY=replace-with-your-deepseek-key
# OMBRE_DEHYDRATION_API_KEY=replace-with-your-deepseek-key
# OMBRE_DEHYDRATION_MODEL=deepseek-chat
# OMBRE_DEHYDRATION_BASE_URL=https://api.deepseek.com/v1

# Optional semantic embeddings
# OMBRE_EMBEDDING_API_KEY=replace-with-your-google-ai-studio-key
# OMBRE_EMBEDDING_MODEL=gemini-embedding-2
# OMBRE_EMBEDDING_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# Optional runtime tuning
# OMBRE_ENRICHMENT_WORKERS=2
# OMBRE_ENRICHMENT_TIMEOUT=45
# OMBRE_EMBEDDING_TIMEOUT=30
# MAX_UPLOAD_BYTES=10485760
# PORT=5000
# BUILD_VERSION=your-release-or-commit-id
# WEB_CONCURRENCY=1
# GUNICORN_WORKER_CLASS=gthread
# GUNICORN_THREADS=8
# GUNICORN_TIMEOUT=300
